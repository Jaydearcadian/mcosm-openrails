# Copy to .dev.vars for local Wrangler development. Never commit .dev.vars.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
OPENRAILS_RUNTIME_ADMIN_TOKEN=replace-with-a-local-only-token
