{
  "name": "openrails-mcp",
  "version": "0.3.1",
  "description": "OpenRails MCP safe-only Shared Interface 1.2 prepare, validate, verify, and read tools over stdio.",
  "license": "MIT",
  "homepage": "https://openrails.pages.dev",
  "type": "module",
  "bin": {
    "openrails-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "openrails",
    "mcp",
    "model-context-protocol",
    "arc",
    "usdc",
    "agent-payments"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ethers": "^6.13.0",
    "openrails-sdk": "^1.1.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}
